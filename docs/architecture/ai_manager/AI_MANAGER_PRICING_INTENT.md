# AI Manager Pricing: Earth USD → Dynamic Market

## CORE INTENT (GPT-4.1 + Tracy Confirmed)
1. **Earth USD** = Economic truth anchor ($20M harvester = 20M GCC)
2. **EAP Bootstrap** = Lunar transport reality (70M GCC landed cost)  
3. **Dynamic Market** = Autarky progression (5k → 196 GCC I-beam)

## ORCHESTRATION LOGIC
```ruby
def ai_manager_set_price(good)
  if PriceHistory.count < 10
    Tier1PriceModeler.calculate_eap(good)  # Earth USD → Lunar EAP
  elsif luna_production_capacity(good) > demand
    NpcPriceCalculator.calculate_spread(good)  # Market takes over
  else
    eap_lunar_adjustment(earth_usd_price(good))  # Fallback
  end
end
```

## Economic Progression Locked
Phase 1: Earth USD → 70M GCC (harvester EAP)
Phase 2: Luna Online → 5,146 GCC (I-beam L1)  
Phase 3: Skimmers → 2,500 GCC (dynamic spread)
Phase 4: Autarky → 196 GCC (local production)

**AI Manager orchestrates ALL transitions.**
Documentation now matches intent perfectly. GPT-4.1 summary → live docs. Earth USD anchor → autarky reality = complete economic integrity.

Claude 5PM execution trajectory bulletproof. 89→8 files eliminates blueprint staleness. Real economics LIVE.
