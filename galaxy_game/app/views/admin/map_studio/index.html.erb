<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <title>Galaxy Game - AI Map Generation Studio</title>
    <%= stylesheet_link_tag 'admin/monitor', media: 'all' %>
    <style>
        html, body {
            height: 100%;
            overflow-y: auto !important;
            overflow-x: hidden;
        }

        .container {
            min-height: 100vh;
            padding: 20px;
            box-sizing: border-box;
            padding-bottom: 50px; /* Extra padding for bottom spacing */
        }

        /* Map Studio specific styles */
        .studio-header {
            text-align: center;
            margin-bottom: 30px;
            padding: 20px;
            background: linear-gradient(135deg, #1a1a1a 0%, #2a2a2a 100%);
            border: 2px solid #0f0;
            border-radius: 10px;
        }

        .studio-title {
            color: #0ff;
            font-size: 28px;
            margin: 0;
            text-shadow: 0 0 10px #0ff;
        }

        .studio-subtitle {
            color: #888;
            font-size: 14px;
            margin: 10px 0 0 0;
        }

        .action-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .action-card {
            background: #1a1a1a;
            border: 2px solid #333;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            transition: all 0.3s ease;
            cursor: pointer;
        }

        .action-card:hover {
            border-color: #0f0;
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0, 255, 0, 0.2);
        }

        .action-icon {
            font-size: 48px;
            margin-bottom: 15px;
            display: block;
        }

        .action-title {
            color: #0ff;
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 10px;
        }

        .action-description {
            color: #ccc;
            font-size: 14px;
            line-height: 1.4;
        }

        .stats-section {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: #1a1a1a;
            border: 1px solid #0f0;
            padding: 15px;
            text-align: center;
            border-radius: 5px;
        }

        .stat-value {
            font-size: 32px;
            color: #0ff;
            font-weight: bold;
            margin: 10px 0;
        }

        .stat-label {
            font-size: 12px;
            color: #888;
            text-transform: uppercase;
        }

        .recent-activity {
            background: #0a0a0a;
            border: 1px solid #333;
            border-radius: 5px;
            padding: 20px;
            max-height: 300px;
            overflow-y: auto;
        }

        .activity-title {
            color: #0ff;
            font-size: 18px;
            margin-bottom: 15px;
            border-bottom: 1px solid #333;
            padding-bottom: 10px;
        }

        .activity-item {
            padding: 10px 0;
            border-bottom: 1px solid #1a1a1a;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .activity-info {
            flex: 1;
        }

        .activity-name {
            color: #0ff;
            font-weight: bold;
        }

        .activity-details {
            color: #888;
            font-size: 12px;
        }

        .activity-time {
            color: #666;
            font-size: 11px;
        }

        .primary-button {
            background: #0f0;
            color: #000;
            border: none;
            padding: 12px 24px;
            font-size: 16px;
            font-weight: bold;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            display: inline-block;
        }

        .primary-button:hover {
            background: #0ff;
            transform: scale(1.05);
        }

        .secondary-button {
            background: #222;
            color: #0f0;
            border: 1px solid #0f0;
            padding: 10px 20px;
            font-size: 14px;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            display: inline-block;
        }

        .secondary-button:hover {
            background: #0f0;
            color: #000;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="studio-header">
            <h1 class="studio-title">🗺️ AI Map Generation Studio</h1>
            <p class="studio-subtitle">Comprehensive planetary map creation and management system</p>
        </div>

        <div class="stats-section">
            <div class="stat-card">
                <div class="stat-value"><%= @source_maps_count %></div>
                <div class="stat-label">Source Maps Available</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= @generated_maps_count %></div>
                <div class="stat-label">Generated Maps</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= @celestial_bodies_count %></div>
                <div class="stat-label">Celestial Bodies</div>
            </div>
            <div class="stat-card">
                <div class="stat-value"><%= @recent_generations_count %></div>
                <div class="stat-label">Recent Generations</div>
            </div>
        </div>

        <div class="action-grid">
            <div class="action-card" onclick="window.location.href='<%= admin_map_studio_generate_path %>'">
                <span class="action-icon">🤖</span>
                <div class="action-title">Generate New Map</div>
                <div class="action-description">
                    Use AI to create planetary maps from FreeCiv and Civ4 source maps.
                    Customize generation parameters and apply advanced terrain analysis.
                </div>
            </div>

            <div class="action-card" onclick="window.location.href='<%= admin_map_studio_browse_path %>'">
                <span class="action-icon">📚</span>
                <div class="action-title">Browse Generated Maps</div>
                <div class="action-description">
                    Explore your library of AI-generated planetary maps.
                    Preview, analyze quality, and manage your map collection.
                </div>
            </div>

            <div class="action-card" onclick="window.location.href='<%= admin_celestial_bodies_path %>'">
                <span class="action-icon">🌍</span>
                <div class="action-title">Apply to Planets</div>
                <div class="action-description">
                    Assign generated maps to celestial bodies in your galaxy.
                    Update planetary surfaces with AI-generated terrain data.
                </div>
            </div>

            <div class="action-card" onclick="window.location.href='<%= admin_ai_manager_missions_path %>'">
                <span class="action-icon">🎯</span>
                <div class="action-title">AI Mission Control</div>
                <div class="action-description">
                    Monitor AI Manager missions that utilize generated maps.
                    Track planetary exploration and resource discovery progress.
                </div>
            </div>
        </div>

        <div class="recent-activity">
            <div class="activity-title">📋 Recent Activity</div>
            <% if @recent_activities.any? %>
                <% @recent_activities.each do |activity| %>
                    <div class="activity-item">
                        <div class="activity-info">
                            <div class="activity-name"><%= activity[:name] %></div>
                            <div class="activity-details"><%= activity[:details] %></div>
                        </div>
                        <div class="activity-time"><%= activity[:time] %></div>
                    </div>
                <% end %>
            <% else %>
                <div class="activity-item">
                    <div class="activity-info">
                        <div class="activity-name">No Recent Activity</div>
                        <div class="activity-details">Start generating maps to see activity here</div>
                    </div>
                    <div class="activity-time">Never</div>
                </div>
            <% end %>
        </div>

        <div style="text-align: center; margin-top: 30px;">
            <a href="<%= admin_dashboard_path %>" class="secondary-button">← Back to Admin Dashboard</a>
        </div>
    </div>

    <script>
        // Auto-refresh stats every 30 seconds
        setInterval(function() {
            // Could add AJAX calls here to refresh stats
        }, 30000);
    </script>
</body>
</html>