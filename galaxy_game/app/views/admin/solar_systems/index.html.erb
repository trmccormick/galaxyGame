<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="csrf-token" content="<%= form_authenticity_token %>">
    <title>Galaxy Game - Admin Solar Systems</title>
    <%= stylesheet_link_tag 'admin/monitor', media: 'all' %>
    <style>
        /* Solar Systems specific styles */
        .galaxy-section {
            margin-bottom: 30px;
            border: 1px solid #0f0;
            padding: 15px;
        }

        .galaxy-header {
            color: #0ff;
            font-size: 18px;
            margin-bottom: 10px;
            border-bottom: 1px solid #333;
            padding-bottom: 5px;
        }

        .system-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 15px;
        }

        .system-card {
            background: #1a1a1a;
            border: 1px solid #333;
            padding: 15px;
            transition: all 0.2s;
        }

        .system-card:hover {
            border-color: #0f0;
            background: #222;
        }

        .system-name {
            color: #0ff;
            font-size: 16px;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .system-stats {
            color: #888;
            font-size: 12px;
            margin-bottom: 8px;
        }

        .system-details {
            color: #0f0;
            font-size: 11px;
        }

        .system-actions {
            margin-top: 10px;
            display: flex;
            gap: 5px;
        }

        .action-btn {
            padding: 5px 10px;
            background: #222;
            border: 1px solid #0f0;
            color: #0f0;
            cursor: pointer;
            font-size: 10px;
            transition: all 0.2s;
        }

        .action-btn:hover {
            background: #0f0;
            color: #000;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 10px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: #1a1a1a;
            border: 1px solid #0f0;
            padding: 15px;
            text-align: center;
        }

        .stat-value {
            font-size: 32px;
            color: #0ff;
            font-weight: bold;
            margin: 10px 0;
        }

        .stat-label {
            font-size: 12px;
            color: #888;
            text-transform: uppercase;
        }
    </style>
</head>
<body>
    <div id="mainContainer" style="grid-template-columns: 250px 1fr; grid-template-rows: 80px 1fr;">
        <!-- Header -->
        <div id="header" style="grid-column: 1 / -1;">
            <h1>GALAXY GAME - SOLAR SYSTEMS ADMIN</h1>
            <div id="planetInfo">
                System Overview: <%= @system_stats[:total_systems] %> Systems | <%= @system_stats[:total_galaxies] %> Galaxies |
                Avg <%= @system_stats[:avg_bodies_per_system] %> Bodies/System
            </div>
        </div>

        <!-- Left Panel: Admin Navigation -->
        <div id="toolPanel">
            <div class="tool-section">
                <h3>🎯 ADMIN SECTIONS</h3>
                <button class="tool-button" onclick="window.location.href='/admin'">
                    📊 Dashboard
                </button>
                <button class="tool-button" onclick="window.location.href='/admin/galaxies'">
                    🌌 Galaxies
                </button>
                <button class="tool-button active" onclick="window.location.href='/admin/solar_systems'">
                    ☀️ Solar Systems
                </button>
                <button class="tool-button" onclick="window.location.href='/admin/celestial_bodies'">
                    🌍 Celestial Bodies
                </button>
                <button class="tool-button" onclick="window.location.href='/admin/organizations'">
                    🏢 Organizations
                </button>
                <button class="tool-button" onclick="window.location.href='/admin/ai_manager/missions'">
                    🤖 AI Manager
                </button>
            </div>

            <div class="tool-section">
                <h3>📊 SYSTEM STATS</h3>
                <div class="stats-grid">
                    <div class="stat-card">
                        <div class="stat-label">Total Systems</div>
                        <div class="stat-value"><%= @system_stats[:total_systems] %></div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">With Stars</div>
                        <div class="stat-value"><%= @system_stats[:systems_with_stars] %></div>
                    </div>
                    <div class="stat-card">
                        <div class="stat-label">Habitable</div>
                        <div class="stat-value"><%= @system_stats[:habitable_systems] %></div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Content Area -->
        <div style="padding: 20px; overflow-y: auto; background: #0a0a0a;">
            <!-- System Statistics -->
            <div class="tool-section">
                <h3>🌌 SOLAR SYSTEMS BY GALAXY</h3>

                <% @galaxies.each do |galaxy| %>
                    <% galaxy_systems = @solar_systems.select { |s| s.galaxy_id == galaxy.id } %>
                    <% next if galaxy_systems.empty? %>

                    <div class="galaxy-section">
                        <div class="galaxy-header">
                            <%= galaxy.name %> (<%= galaxy.galaxy_type&.humanize || 'Unknown Type' %>)
                            - <%= galaxy_systems.count %> Systems
                        </div>

                        <div class="system-grid">
                            <% galaxy_systems.each do |system| %>
                                <div class="system-card">
                                    <div class="system-name"><%= system.name %></div>
                                    <div class="system-stats">
                                        <%= system.celestial_bodies.count %> Bodies |
                                        <%= system.stars.count %> Stars |
                                        ID: <%= system.identifier %>
                                    </div>
                                    <div class="system-details">
                                        <% if system.stars.first %>
                                            Star: <%= system.stars.first.name %> (<%= system.stars.first.type_of_star || 'Unknown' %>)
                                        <% else %>
                                            No central star detected
                                        <% end %>
                                    </div>
                                    <div class="system-actions">
                                        <button class="action-btn" onclick="window.location.href='/admin/solar_systems/<%= system.id %>'">
                                            View Details
                                        </button>
                                        <button class="action-btn" onclick="window.location.href='/solar_systems/<%= system.id %>'">
                                            Public View
                                        </button>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                <% end %>

                <!-- Systems without galaxies -->
                <% orphaned_systems = @solar_systems.select { |s| s.galaxy.nil? } %>
                <% if orphaned_systems.any? %>
                    <div class="galaxy-section">
                        <div class="galaxy-header">
                            Ungrouped Systems - <%= orphaned_systems.count %> Systems
                        </div>

                        <div class="system-grid">
                            <% orphaned_systems.each do |system| %>
                                <div class="system-card">
                                    <div class="system-name"><%= system.name %></div>
                                    <div class="system-stats">
                                        <%= system.celestial_bodies.count %> Bodies |
                                        <%= system.stars.count %> Stars |
                                        ID: <%= system.identifier %>
                                    </div>
                                    <div class="system-details">
                                        <% if system.stars.first %>
                                            Star: <%= system.stars.first.name %> (<%= system.stars.first.type_of_star || 'Unknown' %>)
                                        <% else %>
                                            No central star detected
                                        <% end %>
                                    </div>
                                    <div class="system-actions">
                                        <button class="action-btn" onclick="window.location.href='/admin/solar_systems/<%= system.id %>'">
                                            View Details
                                        </button>
                                        <button class="action-btn" onclick="window.location.href='/solar_systems/<%= system.id %>'">
                                            Public View
                                        </button>
                                    </div>
                                </div>
                            <% end %>
                        </div>
                    </div>
                <% end %>

                <% if @solar_systems.empty? %>
                    <div style="text-align: center; padding: 40px; color: #666;">
                        No solar systems found.<br>
                        <button class="tool-button" style="margin-top: 20px;" onclick="window.location.href='/simulation'">
                            Initialize Simulation
                        </button>
                    </div>
                <% end %>
            </div>
        </div>
    </div>
</body>
</html>