class AddInsuranceCorporationToOrganizations < ActiveRecord::Migration[7.0]
  def change
    # This migration is handled by the enum update in the model
    # No database changes needed as enum values are stored as integers
  end
end